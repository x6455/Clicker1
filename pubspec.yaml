name: starterapp
description: A new Flutter project.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.7
  dio: ^5.4.0
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  provider: ^6.1.5
  shared_preferences: ^2.2.2
  dart_jsonwebtoken: ^2.10.0
  internet_connection_checker_plus: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/
    - assets/images/
